#include "Drawable.hpp"

// Drawable::Drawable(
// 	Buffer vertexBuffer, Buffer indexBuffer, MaterialInstance material
// ) :
// 	m_vertexBuffer(vertexBuffer),
// 	m_indexBuffer(indexBuffer),
// 	m_materialInstance(material) {}